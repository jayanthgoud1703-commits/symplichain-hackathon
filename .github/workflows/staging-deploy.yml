name: Deploy to Staging

on:
  push:
    branches:
      - staging

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Build Frontend
        run: |
          echo "Building React App"

      - name: Deploy Step
        run: |
          echo "Deploying to EC2 and S3"
